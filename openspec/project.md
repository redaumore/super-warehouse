# Project: super-warehouse

## Status
- **Stack**: greenfield / undecided — no source code, no build tooling, no framework chosen.
- **Persistence mode**: hybrid (OpenSpec file artifacts + Engram observations).
- **Strict TDD**: false (no test runner available).

## Context
Greenfield project for a warehouse / ferretería (hardware store) management system
(MVP). The repository is NOT yet a git repository and contains no source code.

## Current repository contents
- `docs/` — two product/technical specification documents (Spanish):
  - `Definición de Producto MVP - Sistema Ferretería.md`
  - `Especificación Técnica y Plan de Implementación.md`
- `.atl/` — tooling registry (`skill-registry.md`, `.skill-registry.cache.json`).
- `.DS_Store`

## MVP goal
Build the MVP of a ferretería (hardware store) management system. The product and
technical specifications live under `docs/` and are the source of requirements until
a change proposal is explored via the SDD workflow.

## Next recommended step
Use `/sdd-new` (or `/sdd-explore`) to turn the MVP goal in `docs/` into a concrete,
scoped change.
